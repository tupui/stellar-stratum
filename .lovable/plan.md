## Cleanup Plan\n\nKnip scan results. Confirmed safe items grouped by impact.\n\n### Unused dependencies — remove\n- `qrcode.react` — no imports anywhere (we use `qrcode` + `jsqr`/`@zxing` instead).\n- `@typescript-eslint/eslint-plugin` and `@typescript-eslint/parser` — superseded by `typescript-eslint` (already used in `eslint.config.js`). Add `typescript-eslint` as an explicit devDependency (currently transitive/unlisted).\n\n### Unused files — delete\n- `src/App.css` — not imported anywhere (only `index.css` is used).\n- `src/components/ui/use-toast.ts` — duplicate re-export shim of `@/hooks/use-toast`; nothing imports it.\n- `src/components/TransactionSummary.tsx` — no remaining references.\n- `src/components/airgap/AnimatedQR.tsx` — superseded by `AnimatedQRScanner`; not imported.\n- `src/lib/xdr/signature-merge.ts` — no references.\n- Keep `public/sw.js` (knip false positive — registered at runtime by `src/lib/service-worker.ts`).\n\n### Duplicate / dead exports — trim\n- `src/pages/AirgapSigner.tsx` — drop the named `export const AirgapSigner`; keep only `export default` (the only consumer is `App.tsx` default import).\n- Remove these unused exports (keep nothing referenced externally):\n  - `src/lib/defindex-client.ts`: `getDefindexNetwork`\n  - `src/lib/error-handling.ts`: `handleError`, `createError`, `ErrorCode`, `AppError`\n  - `src/lib/fiat-currencies.ts`: `getFxRate`\n  - `src/lib/horizon-utils.ts`: `MIN_NATIVE_PAYMENT_XLM`, `ALLOWED_TYPES`\n  - `src/lib/reflector-client/index.ts` + `xdr-helper.ts`: `buildAssetScVal`, `parseSorobanResult`, `AssetTypeValue`\n  - `src/lib/reflector.ts`: `getLastPriceUpdate`, `AssetPrice` (if truly unused)\n  - `src/lib/rpc-client.ts`: `getRpcUrl`, `createRpcServer`, `clearRpcCache`, `NetworkType` (keep only the `createOracleRpcServer`/`createDomainsRpcServer` actually used)\n  - `src/lib/stellar.ts`: `horizonServer` (legacy compat shim), `AccountData` if unused\n  - `src/lib/validation.ts`: `isValidXdr`, `isValidAssetCode`\n  - `src/lib/xdr/parse.ts`: `getSourceAccount`, `isSuccessfulResultXdr`, `ParsedTransaction` (verify)\n  - `src/lib/qr.ts`: `QRResult`\n  - `src/lib/sep7.ts`: `SEP7TxPayload`\n  - `src/hooks/use-toast.ts`: internal `reducer` export\n  - shadcn ui leftovers used nowhere: `AlertTitle`, `badgeVariants`, `BadgeProps`, `CardFooter`, `DialogPortal/Overlay/Close/Trigger/Footer`, `SelectGroup/Label/Separator/ScrollUp/ScrollDownButton`, `ToastAction`, `Toggle`, `ButtonProps`, `CalendarProps`, `TextareaProps`\n  - For shadcn primitives I'll only remove exports that are truly unused — these files are otherwise kept intact so future shadcn additions still work.\n\n### Verification\n- After each batch: `bunx tsc -b --noEmit` and `bun run build:dev`.\n- Re-run `bunx knip` to confirm a clean tree (only intentional false positives remain).\n\n### Notes / out of scope\n- Keep