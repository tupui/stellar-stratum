// Empty module stub for native dependencies not needed in browser
// Used to replace 'usb' package which requires node-gyp compilation
// Browser uses WebUSB API instead via @ledgerhq/hw-transport-webusb

export default {};
